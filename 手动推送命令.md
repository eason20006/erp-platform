# 手动推送命令（如果脚本有问题）

如果脚本执行失败，可以手动执行以下命令：

## 📝 完整命令序列

在终端中，**一行一行**执行：

```bash
# 1. 进入项目目录
cd /Users/wangxiyue/软件测试/练习网站

# 2. 初始化 Git（强制重新初始化）
rm -rf .git
git init

# 3. 添加所有文件
git add .

# 4. 提交
git commit -m "Initial commit: ERP platform"

# 5. 连接远程仓库
git remote add origin https://github.com/eason20006/erp-platform.git

# 6. 设置主分支
git branch -M main

# 7. 推送到 GitHub（需要认证）
git push -u origin main
```

---

## ⚠️ 推送时需要认证

执行 `git push` 时会要求输入：

- **Username**: `eason20006`
- **Password**: 使用 Personal Access Token（不是密码）

### 获取 Token：

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. Note: `Render Deployment`
4. 勾选 `repo` 权限
5. 点击 "Generate token"
6. **复制 token**（只显示一次）

推送时，在 Password 处**粘贴 token**。

---

## ✅ 验证推送成功

推送成功后，访问：
https://github.com/eason20006/erp-platform

应该能看到所有文件。
