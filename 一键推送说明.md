# 一键推送代码到 GitHub

## 🚀 快速执行

我已经创建了自动推送脚本，请在**终端**中执行：

```bash
cd /Users/wangxiyue/软件测试/练习网站
./push_to_github.sh
```

---

## 📝 脚本会自动完成：

1. ✅ 初始化 Git 仓库（如果还没有）
2. ✅ 连接 GitHub 远程仓库
3. ✅ 添加所有文件
4. ✅ 提交更改
5. ✅ 推送到 GitHub

---

## ⚠️ 推送时需要认证

脚本执行到推送步骤时，会要求输入：

### 方法 1：使用 Personal Access Token（推荐）

1. **获取 Token**：
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token" → "Generate new token (classic)"
   - Note: `Render Deployment`
   - 勾选权限：`repo`（所有仓库权限）
   - 点击 "Generate token"
   - **复制 token**（只显示一次！）

2. **推送时使用 Token**：
   - 当脚本提示输入密码时
   - Username: `eason20006`
   - Password: **粘贴 token**（不是密码）

### 方法 2：使用 GitHub CLI（如果已安装）

```bash
gh auth login
```

---

## 🔄 如果推送失败

### 错误：需要认证

**解决方法**：使用 Personal Access Token（见上方）

### 错误：仓库已存在内容

**解决方法**：先拉取再推送

```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### 错误：权限被拒绝

**解决方法**：
1. 检查 GitHub 用户名是否正确
2. 确认仓库地址：`https://github.com/eason20006/erp-platform.git`
3. 使用 Personal Access Token

---

## ✅ 推送成功的标志

1. ✅ 终端显示：`Branch 'main' set up to track remote branch 'main'`
2. ✅ 访问 https://github.com/eason20006/erp-platform 能看到所有文件
3. ✅ 可以看到 `erp_platform/`、`wsgi.py`、`gunicorn_config.py` 等文件

---

## 🎯 完整流程

```
1. 执行脚本：./push_to_github.sh
   ↓
2. 输入 GitHub 认证（用户名 + Token）
   ↓
3. 推送成功
   ↓
4. 访问 GitHub 仓库确认文件
   ↓
5. 返回 Render 平台部署
```

---

## 💡 提示

- 脚本会自动处理大部分操作
- 只需要在推送时提供认证信息
- 如果遇到问题，查看错误信息并告诉我

**现在请在终端执行：`./push_to_github.sh`** 🚀
