# Render 配置界面详细说明

## 📍 配置位置说明

当你点击 "New +" → "Web Service" 并选择仓库后，会进入 **"Create New Web Service"** 页面。

这个页面分为几个部分，配置项的位置如下：

---

## 🎯 配置项位置（从上到下）

### 1️⃣ 基本信息区域（页面顶部）

#### Name（名称）
- **位置**：页面最上方，第一个输入框
- **标签**：显示为 "Name" 或 "Service Name"
- **填写**：`erp-platform`

#### Region（区域）
- **位置**：Name 下方，下拉选择框
- **标签**：显示为 "Region"
- **选择**：`Singapore`（推荐）或 `Oregon`

#### Branch（分支）
- **位置**：Region 下方，下拉选择框或输入框
- **标签**：显示为 "Branch"
- **选择**：`main` 或 `master`

#### Root Directory（根目录）
- **位置**：Branch 下方，输入框（可选）
- **标签**：显示为 "Root Directory"
- **填写**：**留空**（如果代码在仓库根目录）

---

### 2️⃣ 环境配置区域（中间部分）

#### Environment（环境）
- **位置**：在 "Environment" 或 "Runtime" 部分
- **标签**：显示为 "Environment" 或 "Runtime"
- **选择**：**"Python 3"**（下拉菜单选择）

---

### 3️⃣ 构建和启动命令区域（重要！）

这个区域通常有标题，如 **"Build & Deploy"** 或 **"Build Settings"**

#### Build Command（构建命令）
- **位置**：在 "Build Command" 或 "Build" 部分
- **标签**：显示为 "Build Command"
- **输入框类型**：文本输入框
- **填写内容**：
  ```
  pip install -r erp_platform/requirements.txt
  ```

#### Start Command（启动命令）
- **位置**：在 "Start Command" 或 "Start" 部分
- **标签**：显示为 "Start Command"
- **输入框类型**：文本输入框
- **填写内容**：
  ```
  gunicorn -c gunicorn_config.py wsgi:app
  ```

**注意**：这两个输入框可能：
- 并排显示（左右排列）
- 上下排列
- 在一个可展开的 "Advanced" 或 "Build Settings" 区域中

---

### 4️⃣ 实例类型区域（页面下方）

#### Instance Type（实例类型）
- **位置**：在 "Instance Type" 或 "Plan" 部分
- **标签**：显示为 "Instance Type" 或 "Plan"
- **选择方式**：单选按钮或下拉菜单
- **选择**：**"Free"**（免费版）

---

## 🔍 如果找不到配置项

### 方法 1：查找 "Advanced" 按钮

有些配置项可能在 **"Advanced"** 或 **"Show Advanced"** 区域：
1. 查找页面上的 **"Advanced"** 按钮或链接
2. 点击展开高级选项
3. Build Command 和 Start Command 可能在这里

### 方法 2：查找 "Build Settings" 标签

有些界面使用标签页：
1. 查找 **"Build Settings"** 或 **"Settings"** 标签
2. 点击切换到该标签
3. 在这里找到 Build Command 和 Start Command

### 方法 3：查找 "Configure" 按钮

如果界面显示的是预览信息：
1. 查找 **"Configure"** 或 **"Edit"** 按钮
2. 点击进入配置页面
3. 在这里填写配置

---

## 📸 界面布局参考

典型的 Render 配置页面布局：

```
┌─────────────────────────────────────┐
│  Create New Web Service             │
├─────────────────────────────────────┤
│  Name: [erp-platform        ]      │ ← 步骤3-1
│  Region: [Singapore ▼]             │ ← 步骤3-2
│  Branch: [main ▼]                   │ ← 步骤3-3
│  Root Directory: [          ]       │ ← 步骤3-4（留空）
├─────────────────────────────────────┤
│  Environment: [Python 3 ▼]         │ ← 步骤3-5
├─────────────────────────────────────┤
│  Build & Deploy                     │
│  ┌───────────────────────────────┐ │
│  │ Build Command:                │ │ ← 步骤3-6
│  │ [pip install -r erp_platform/ │ │
│  │  requirements.txt        ]    │ │
│  └───────────────────────────────┘ │
│  ┌───────────────────────────────┐ │
│  │ Start Command:                 │ │ ← 步骤3-7
│  │ [gunicorn -c gunicorn_config. │ │
│  │  py wsgi:app            ]      │ │
│  └───────────────────────────────┘ │
├─────────────────────────────────────┤
│  Instance Type:                     │
│  ○ Free  ○ Starter  ○ Standard     │ ← 步骤3-8（选Free）
├─────────────────────────────────────┤
│           [Create Web Service]      │ ← 步骤4
└─────────────────────────────────────┘
```

---

## ✅ 配置检查清单

填写时确认：

- [ ] **Name**: 已填写（如：`erp-platform`）
- [ ] **Environment**: 已选择 **"Python 3"**
- [ ] **Build Command**: 已填写 `pip install -r erp_platform/requirements.txt`
- [ ] **Start Command**: 已填写 `gunicorn -c gunicorn_config.py wsgi:app`
- [ ] **Instance Type**: 已选择 **"Free"**

---

## 🆘 如果还是找不到

### 可能的情况：

1. **界面版本不同**：
   - Render 界面可能更新了
   - 查找类似的标签或按钮

2. **配置项名称不同**：
   - Build Command 可能叫 "Build"
   - Start Command 可能叫 "Start" 或 "Run Command"

3. **需要先选择 Environment**：
   - 先选择 "Python 3"
   - 然后 Build Command 和 Start Command 才会显示

### 建议：

1. **截图当前界面**，我可以帮你定位
2. **查找关键词**：
   - "Build"
   - "Start"
   - "Command"
   - "Deploy"
   - "Settings"

---

## 💡 提示

- 如果看到 **"Auto-deploy"** 选项，可以保持默认（开启）
- 如果看到 **"Health Check Path"**，可以留空
- 如果看到 **"Environment Variables"**，这是可选的，可以先跳过

**最重要的是确保 Build Command 和 Start Command 填写正确！**
